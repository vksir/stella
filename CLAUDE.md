## Agent skills

### Issue tracker

issue 以本地 markdown 文件形式存在 `.scratch/`。详见 `docs/agents/issue-tracker.md`。

### Domain docs

单上下文（single-context），`CONTEXT.md` + `docs/adr/` 位于仓库根目录。详见 `docs/agents/domain.md`。
